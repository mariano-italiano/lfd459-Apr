apiVersion: v1
kind: Pod
metadata:
  name: pv-pod
  labels:
    app: pvpod
spec:
  containers:
    - name: web
      image: nginx
      volumeMounts:
      - mountPath: "/usr/share/nginx/html"
        name: my-vol
  volumes:
    - name: my-vol
      persistentVolumeClaim:
        claimName: pvc 
